spring:
  datasource:
    driver-class-name: org.h2.Driver
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

services:
  business-payment:
    url: http://localhost:9001
  invoice:
    url: http://localhost:9002