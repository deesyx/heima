spring:
  datasource:
    driver-class-name: org.h2.Driver
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

services:
  rental-delegation:
    url: http://localhost:8081
